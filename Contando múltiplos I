#include <stdio.h>

int main(){
    int x, n;

    scanf("%d", &x);
    scanf("%d", &n);

    int i = 1;
    int multiplosX = 0;

    while(multiplosX < n){
        multiplosX = x * i;
        i++;
    }

    printf("O numero %d tem %d multiplos menores que %d.\n", x, i-2, n);

    return 0;
}
