#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int s;
    float final, total;

    scanf("%d", &s);
    total = (0.075) * s;
    final = (int)s + total;
    
    printf("%0.2f", final);
    return 0;
}
