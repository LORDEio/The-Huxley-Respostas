#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
void reduz (double *a, double *b);
int main() {
   double x, y;
   scanf("%lf%lf", &x, &y);
   reduz(&x, &y);
   printf("%.2lf %.2lf\n", x, y);
   return 0;
}
void reduz (double *a, double *b)
{
    *a =*a - *b;
}
