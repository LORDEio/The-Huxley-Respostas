#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int n1, n2 = 0;
    
    scanf("%i%i", &n1, &n2);
    
    for(int i = n1; i <= n2; i++)
    {
        if(i % 2 != 0)
        {
            printf("%i\n", i);
        }
    }
    return 0;
}
