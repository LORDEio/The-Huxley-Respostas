
#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()
{
     float a, b, c, raiz1, raiz2, delta;

                scanf("%f", &a);
                scanf("%f", &b);
                scanf("%f", &c);

           delta = b*b - 4*a*c;
           raiz1 = (-b + sqrt(delta)) / (2*a);
           raiz2 = (-b - sqrt(delta)) / (2*a);

        if(a==0)
            {
            printf("NEESG");
            }
            else{
                if(delta < 0)
                {
                    printf("NRR");
                }
                        else
                        {
                            printf("%.2f\n", raiz1);
                            printf("%.2f\n", raiz2);
                        }
                }

 return 0;
}
