#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     float am, amen, h, total;
           scanf("%f""%f""%f", &am, &amen, &h);
            
            total = ((am + amen)*h)/2;
                 
                 printf("%.1f", total);
	return 0;
}
