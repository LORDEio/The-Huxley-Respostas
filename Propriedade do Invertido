#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    
        int x, y, i, c1, c2;
        scanf("%d %d", &x, &y);
                
                for(i=x ;i <= y ;i = i +1)
                {
                    c1 = i % 10;
                    c2 = i / 10;
            
                    if(c1 != 0 && i % c2 == 0)
                    {
                        printf("%d\n", i);
                    }
                    
                }
                
	return 0;
}
